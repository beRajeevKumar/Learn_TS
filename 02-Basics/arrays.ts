const superHero: string[] = [];
// const heroPower: number[] = [];
const heroPower: Array<number> = [];

type User = {
  name: string;
  isActive: boolean;
};
const allUsers: User[] = [];

const MLModels: string[][] = [
  ["model1", "model2"],
  ["model3", "model4"],
];

superHero.push("Superman");
heroPower.push(100);
allUsers.push({ name: "Clark Kent", isActive: true });

export {};
